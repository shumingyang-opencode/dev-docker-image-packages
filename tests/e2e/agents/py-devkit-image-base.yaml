image: py-devkit-image-base
ghcr_repo: ghcr.io/shumingyang-opencode/py-devkit-image-base

agents:
  - name: opencode
    binary: opencode
    env_vars: [OPENCODE_API_KEY]
    run_cmd: 'opencode run {prompt}'

  - name: copilot
    binary: copilot
    env_vars: [COPILOT_GITHUB_TOKEN, GH_TOKEN, GITHUB_TOKEN]
    run_cmd: 'echo {prompt} | copilot'

  - name: lark-cli
    binary: lark-cli
    env_vars: [LARKSUITE_CLI_APP_ID, LARKSUITE_CLI_APP_SECRET]
    run_cmd: 'export LARKSUITE_CLI_BRAND=feishu LARKSUITE_CLI_DEFAULT_AS=bot LARKSUITE_CLI_STRICT_MODE=bot && lark-cli whoami'

  - name: trae-cli
    binary: trae-cli
    supports_model: true
    env_vars: [OPENAI_API_KEY, ANTHROPIC_API_KEY, OPENROUTER_API_KEY, DOUBAO_API_KEY]
    run_cmd: 'trae-cli run {prompt}'
